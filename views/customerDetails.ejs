<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Candy Shop - Admin Page</title>
        <link rel="stylesheet" href="/styles/myAccount.css ">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    </head>
    <body>  
        <!-- Topbar -->
        <div class="top-bar">
            <!-- Left elements -->
            <div class="left-section">
                <img src="/images/shooping_cart.svg" alt="Icon 1" class="icon">
                <img src="/images/logout.svg" alt="Icon 1" class="icon">
            </div>
            <!-- Logo -->
            <div class="center-section">
                <img src="/images/logo.svg" class="logo"> 
            </div>
            <!-- Right elements -->
            <div class="right-section">
                <img src="/images/home_icon.svg" alt="Icon 1" class="icon" onclick="window.location.href='/home'">
            </div>
        </div>
        <!-- Account Section -->
        <div class="account-section">
            <div class="account-header">
                <h2>Hello <%= customer.name %></h2>
            </div>
            <div class="account-content">
                <!-- Sub Menu -->
                <div class="account-menu">
                    <button class="menu-option active" onclick="window.location.href='/myAccount/details'">Personal Details</button>
                    <button class="menu-option" onclick="window.location.href='/myAccount/favorite'">Favorits</button>
                    <button class="menu-option" onclick="window.location.href='/myAccount/orders'">My Orders</button>
                </div>
                <!-- Account Details -->
                <div class="content">
                    <!-- Details -->
                    <div id="details" class="content-item active">
                        <div class="container">
                            <!-- Form with Details -->
                            <form action="/myAccount" method="POST" id="updateForm">
                                <div class="form-row">
                                    <div class="form-column">
                                        <div class="form-group">
                                            <label for="name">Name</label>
                                            <input type="text" id="name" name="name" value="<%= customer.name %>" required>
                                        </div>
                                        <div class="form-group">
                                            <label for="email">Email</label>
                                            <input type="email" id="email" name="email" value="<%= customer.email %>" required>
                                        </div>
                                        <div class="form-group">
                                            <label for="birthdate">Birthday</label>
                                            <div class="birthday-container">
                                                <select id="day" name="birth_day" required class="small-input">
                                                    <% for (let i = 1; i <= 30; i++) { %>
                                                        <option value="<%= i %>" <%= customer.birthdate && new Date(customer.birthdate).getDate() === i ? 'selected' : '' %>><%= i %></option>
                                                    <% } %>
                                                </select>
                                                <select id="month" name="birth_month" required class="small-input">
                                                    <% for (let i = 1; i <= 12; i++) { %>
                                                        <option value="<%= i %>" <%= customer.birthdate && (new Date(customer.birthdate).getMonth() + 1) === i ? 'selected' : '' %>><%= i %></option>
                                                    <% } %>
                                                </select>
                                                <select id="year" name="birth_year" required>
                                                    <% for (let i = 1990; i <= 2024; i++) { %>
                                                        <option value="<%= i %>" <%= customer.birthdate && new Date(customer.birthdate).getFullYear() === i ? 'selected' : '' %>><%= i %></option>
                                                    <% } %>
                                                </select>
                                            </div>
                                        </div>
                                        
                                    </div>
                                    <div class="form-column">
                                        <div class="form-group">
                                            <label for="phone">Phone Number</label>
                                            <input type="tel" id="phone" name="phone" value="<%= customer.phone %>" pattern="[0-9]{3}-[0-9]{7}" maxlength="10" required>
                                        </div>
                                        <div class="form-group">
                                            <label for="address">Address:</label>
                                            <div class="address-container">
                                                <input type="text" id="street" name="address_street" value="<%= customer.address ? customer.address.street : '' %>" required placeholder="Street">
                                                <input type="text" id="number" name="address_number" value="<%= customer.address ? customer.address.number : '' %>" required placeholder="Number" class="small-input">
                                                <input type="text" id="city" name="address_city" value="<%= customer.address ? customer.address.city : '' %>" required placeholder="City">
                                            </div>
                                        </div>
                                        
                                        <div class="form-group">
                                            <label for="password">Password</label>
                                            <div class="password-container">
                                                <input type="password" id="password" name="password" value="<%= customer.password %>" required>
                                                <i class="fas fa-eye" id="togglePassword"></i>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                                <div class="form-group full-width">
                                    <button id="submitButton" type="submit" class="btn">Save Changes</button>
                                </div>
                            </form>
                        </div>
                    </div>
                    <!-- Favorite -->
                    <div id="favorit" class="content-item" style="display:none;">This is the content for Option 2.</div>
                    <!-- Orders -->
                    <div id="orders" class="content-item" style="display:none;">This is the content for Option 3.</div>
                </div>
            </div>
        </div>
        <script src="/js/myAccount.js"></script>
        <script>
            // JavaScript to submit the form programmatically when the button is clicked
            document.getElementById('submitButton').addEventListener('click', function() {
                document.getElementById('updateForm').submit();  // Submit the form
            });
        </script>
    </body>
</html>